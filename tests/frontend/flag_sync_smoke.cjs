// Playwright smoke test — stub. TODO(Phase 2).
// Mirrors LLama-GUI's flag_sync_smoke.cjs: serve ui/ as web root, stub backend
// APIs, verify Generate<->Configure state sync, command preview, preview
// polling, history restore, preset save/load. See PLAN.md §17.
"use strict";

console.log(
	"flag_sync_smoke.cjs: not implemented until Phase 2 (see PLAN.md §17).",
);
process.exit(0);
