// Preset CRUD + import/export. Presets = generation configs grouped by model type.
// Mirrors LLama-GUI's presets.js. TODO(Phase 4).
window.SDGui = window.SDGui || {};

window.SDGui.presets = {
	init: () => {
		// TODO(Phase 4): list/save/load/delete/import/export via /api/presets.
	},
};
