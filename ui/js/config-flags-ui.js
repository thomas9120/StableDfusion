// Configure tab rendering: search/filter, type-specific inputs, command preview.
// Reads/writes launch state through window.SDGui.flagCore. TODO(Phase 4).
window.SDGui = window.SDGui || {};

window.SDGui.configFlagsUi = {
	init: () => {
		// TODO(Phase 4): render FLAG_CATEGORIES accordions with per-flag inputs,
		// wire search, expand/collapse, custom launch args, command preview.
	},
	render: () => {},
};
