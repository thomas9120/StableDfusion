// Hugging Face downloader UI: repo id, multi-file selectors, progress, cancel.
// Mirrors LLama-GUI's hf-download-ui.js. TODO(Phase 3).
window.SDGui = window.SDGui || {};

window.SDGui.hfDownloadUi = {
	init: () => {
		// TODO(Phase 3): Find Files → /api/hf/repo-files, multi-file select,
		// Download → /api/hf/download, poll /api/hf/download-status, cancel.
	},
};
