// Persistent sd-server tab: start/stop, status, endpoint links. TODO(Phase 5).
window.SDGui = window.SDGui || {};

window.SDGui.serverUi = {
	init: () => {
		// TODO(Phase 5): /api/sd-server/start|stop|status, poll status badge.
	},
};
