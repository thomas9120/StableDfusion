// API endpoint snippets for the running sd-server (/sdcpp/v1, /v1, /sdapi/v1).
// Mirrors LLama-GUI's api-tab.js. TODO(Phase 5).
window.SDGui = window.SDGui || {};

window.SDGui.apiTab = {
	init: () => {
		// TODO(Phase 5): render endpoint docs/snippets from the live sd-server target.
	},
};
