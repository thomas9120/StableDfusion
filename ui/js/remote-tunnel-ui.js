// Cloudflare tunnel UI: start/stop, URL + copy, status polling.
// ~Verbatim from LLama-GUI's remote-tunnel-ui.js. TODO(Phase 5).
window.SDGui = window.SDGui || {};

window.SDGui.remoteTunnelUi = {
	init: () => {
		// TODO(Phase 5): /api/remote-tunnel/start|stop|status, poll, render URL.
	},
};
